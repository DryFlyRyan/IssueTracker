import React from 'react';
import PropTypes from 'prop-types';

const RepoCard = ({
  isActive,
  name,
  created,
  className,
}) => {
  return (
    <div
      className={className}
    >
      
    </div>
  );
};

export default RepoCard;
